rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Regla de producción temporal: permite lectura y escritura a cualquiera
      // ya que la aplicación actualmente no requiere autenticación de usuario.
      allow read, write: if true;
    }
  }
}
